import styled from 'styled-components';

export const SocialWrapper = styled.div `
    display: flex;
    gap: 1rem;
    justify-content: center;
    align-items: center;
    img{
        width: 60px;
    }
`
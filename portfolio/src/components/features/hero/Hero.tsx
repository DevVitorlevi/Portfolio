import HeroSection from "./heroSection/HeroSection"

const Hero = () => {
    return (
        <section id="hero" className='h-[100vh]'>
            <HeroSection />
        </section>
    )
}

export default Hero
